@model WebApp.Models.Auth.IsAuthModel

@if (!Model.IsAuthen)
{
    @Html.ActionLink("Войти", "Login", "Auth", new { area = "" }, new { @class = "nav-link" });
}
else
{
    @Html.ActionLink("Выйти", "Logout", "Auth", new { area = "" }, new { @class = "nav-link" });
}